

const FeturedBlog = ({ feturedBlog }) => {
  console.log(feturedBlog);
 
  return (
    <div>
     
    </div>
  );
};

export default FeturedBlog;

// {top10Blogs.map((feturedBlog) => (
//     <FeturedBlog
//       key={feturedBlog._id}
//       feturesBlog={feturedBlog}
//     ></FeturedBlog>
//   ))}
